{
  "name": "mono-cloudflare-app",
  "scripts": {
    "build": "npm run build:frontend && npm run build:blog",
    "build:frontend": "cd frontend && npm install && npm run build && mkdir -p dist && cp -r dist/* ../dist/",
    "build:blog": "cd blog && npm install && npm run build && mkdir -p ../dist/blog && cp -r dist/* ../dist/blog/"
  }
}
